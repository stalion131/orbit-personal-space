# Журнал проекта

## 2026-08-31 — Dashboard MVP

- Создан локальный дашборд задач с шестью сферами колеса баланса и согласованными подкатегориями.
- Добавлены срок, очередность, приоритет, поиск, фильтры, история шагов, экспорт и ручное завершение.
- Добавлен безопасный деморежим подтверждений; он не вызывает LLM, поиск, почту, бронирование или оплату.
- Подключено устойчивое локальное хранение и конкурентный контроль по ревизии.
- Стиль адаптирован по `D:\Projects\ai-training-coach`: navy-фон, градиентные панели, оранжевые действия и цветовые акценты сфер.
- Проверено: целевой lint/typecheck, production build, чтение шести сфер из базы, сроки/приоритеты у всех демо-задач, переход `ожидает → подтверждение → решение`, отклонение устаревшего решения.
- Зависимости: high/critical уязвимости устранены. Остались 4 moderate в dev-зависимости Drizzle Kit; предложенное npm-исправление понижает пакет с несовместимым major и поэтому не применено.
- Ограничение: это локальный MVP без аккаунтов и ИИ-агентов. Не публиковать до добавления аутентификации и модели доступа.

Следующий рекомендуемый шаг: подключить минимальный Python-оркестратор на OpenAI Agents SDK без инструментов с побочными эффектами.

## 2026-08-31 — Пользовательские задачи

- Полностью удалены демонстрационные записи из локальной базы.
- Добавлены 17 пользовательских задач по сферам «Работа», «Личные задачи» и «Встречи».
- Добавлены направления «Квартира» и «Сфера строительства / Текущие проекты», а также новая сфера «Встречи».
- Очередность соответствует порядку исходного списка. Для задач без указанной даты срок оставлен пустым.
- Ближайшие задачи на 1 и 4 сентября отмечены критическими; остальные получили консервативный приоритет по срочности.

## 2026-09-01 — Подготовка облачной и мобильной версии

- Добавлен режим Supabase Auth/Postgres с серверной проверкой JWT и RLS по владельцу; `service_role` не используется.
- Добавлена SQL-миграция с явными grants и отдельными политиками select/insert/update.
- Локальный D1 сохранён как режим разработки и отката; удалённый доступ к нему по-прежнему запрещён.
- Добавлен валидируемый импорт `orbit-tasks-v1`. Локально проверено: 17 задач до импорта, 17 принято, 17 осталось после идемпотентного повторного импорта.
- Добавлены manifest, три PWA-иконки, безопасный service worker и инструкции установки на Android/iPhone. API и страницы с задачами не кэшируются.
- Исправлено горизонтальное переполнение на мобильной ширине; при 375 px `scrollWidth` совпадает с `clientWidth`.
- Проверено: production build, lint, локальная загрузка 17 задач, блокировка чужого Origin (403), manifest/service worker (200), мобильный интерфейс без ошибок консоли.
- Ограничение: live Supabase/RLS и production-ссылка ещё не проверены — нет отдельного проекта/ключей и решения владельца по размещению данных. Hosted Supabase не имеет российского региона; реальные задачи туда не передавались.

Следующий шаг: владелец выбирает допустимое размещение. После выбора — создать отдельный проект, выполнить миграцию, проверить изоляцию двумя аккаунтами, установить runtime-переменные Sites и развернуть owner-only версию.

## 2026-09-01 — Supabase и приватная публикация

- По прямому решению владельца создан отдельный бесплатный Supabase-проект `orbit-personal-space` в регионе Central EU (Frankfurt), `eu-central-1`.
- Автоматическая публикация новых таблиц отключена, автоматический RLS включён. Применена версионируемая миграция `202609010001_orbit_tasks.sql`.
- Контрольный запрос подтвердил: RLS включён, действуют 3 политики владельца, у `anon` нет select/insert, у `authenticated` есть только select/insert/update, delete запрещён.
- Создан отдельный publishable key для веб-дашборда. Secret/service-role ключи не раскрывались, не сохранялись и приложению не передавались.
- В Sites добавлены runtime-переменные Supabase и опубликована версия 1 с owner-only доступом. Вход через ChatGPT и загрузка формы Supabase проверены в production.
- Публичная версия не включалась; разрешён только аккаунт владельца. Локальный backup из 17 задач сохранён за пределами сборки в `outputs/orbit-tasks-backup.json`.
- Ограничение: облачный аккаунт Orbit ещё не создан, поэтому перенос 17 задач и проверка сохранения между устройствами ожидают пароль, который должен задать владелец.

Следующий шаг: создать аккаунт Orbit в опубликованном приложении, подтвердить email при необходимости, импортировать backup и проверить сохранение после повторного входа с телефона.

## 2026-09-01 — Публикация в Vercel

- Приложение перенесено на Next.js для совместимого развёртывания в Vercel. Cloudflare-специфичный D1-адаптер удалён из production-пути; без Supabase локально используется только временное хранение в памяти.
- В Vercel добавлены только `SUPABASE_URL` и `SUPABASE_PUBLISHABLE_KEY` для Production, Preview и Development. `service_role` не передавался.
- Создан production-выпуск проекта `orbit-personal-space`; постоянный адрес: `https://orbit-personal-space.vercel.app`.
- Проверено: `npm run lint`, `npm run build`, успешная сборка Vercel и production-форма входа, которая требует аккаунт Supabase и сообщает об изоляции задач по владельцу.
- Ограничение: аккаунт Orbit ещё не создан, поэтому backup с 17 задачами пока не импортирован в облако.

Следующий шаг: создать аккаунт в Orbit, при необходимости подтвердить email, импортировать `outputs/orbit-tasks-backup.json` и проверить вход с телефона.

## 2026-09-01 — Планировщик сфер и задач

- Список задач переработан: задачи сгруппированы заголовками по сферам и направлениям; крупная типографика и touch-friendly элементы адаптированы для телефона.
- Добавлены редактирование, ручное завершение и удаление задач, приоритетные метки и галочка «разобрать первой». Такие задачи всегда показаны выше остальных.
- Колесо баланса теперь считает активные задачи динамически по сферам и отдельно выделяет незавершённые задачи со сроком сегодня.
- Добавлен управляемый каталог: пользователь может добавлять, переименовывать, менять цвет и перетаскивать сферы, а также добавлять и менять направления. Направление жёстко закреплено за одной сферой.
- Подготовлена миграция `202609010002_orbit_catalogs.sql`: каталог хранится отдельно по владельцу, добавлена безопасная RLS-политика удаления собственных задач.
- Проверено локально: получение каталога, создание задачи, отметка первой, редактирование и удаление; `npm run lint` и `npm run build` успешны.

Следующий шаг: применить новую миграцию в Supabase и развернуть эту версию в Vercel.

## 2026-09-01 — Облачная миграция каталога

- Миграция `202609010002_orbit_catalogs.sql` применена в Supabase-проекте `orbit-personal-space`; SQL Editor подтвердил успешное выполнение.
- В существующий Vercel-проект нельзя загрузить новый архив через Vercel Drop: сервис предотвращает создание проекта с тем же именем. Новый публичный проект намеренно не создавался, чтобы не разрывать основной адрес.

Следующий шаг: подключить существующий Vercel-проект к Git-репозиторию либо авторизовать Vercel CLI, после чего новую версию можно опубликовать по текущему адресу.

## 2026-09-01 — GitHub и автоматические публикации

- Создан приватный репозиторий `stalion131/orbit-personal-space`; исходный код приложения отправлен в ветку `main` без секретов и резервной копии пользовательских задач.
- Существующий Vercel-проект `orbit-personal-space` подключён к этому репозиторию. Последующие коммиты в `main` автоматически создают Production-развёртывание на прежнем постоянном адресе.
- Импорт старой резервной копии исправлен: существующие подкатегории задач автоматически сопоставляются направлениям нового каталога.
- Перед публикацией повторно выполнены `npm run lint` и `npm run build`; обе проверки успешны.

Следующий шаг: дождаться первого Git-развёртывания, проверить постоянный адрес и импортировать 17 задач после входа в Orbit.

## 2026-09-01 — Исправление Production-оформления

- Первый Git-деплой повторно использовал старый CSS-кэш Vercel Drop: новая логика и 20 облачных задач загрузились, но стили планировщика отсутствовали.
- CSS-кэш принудительно инвалидирован изменением таблицы стилей; новая Production-сборка опубликована со статусом Ready.
- Проверены фактические вычисленные стили и внешний вид при ширине 619 px и 1280 px: мобильная и настольная раскладки восстановлены, колесо снова круглое, карточки и формы не накладываются.
- Пользовательские данные не изменялись.

Следующий шаг: распределить уже импортированные задачи по направлениям каталога и проверить редактирование на телефоне.

## 2026-09-01 — Блокнотный дашборд и календарное планирование

- Интерфейс переработан в стиле рабочего блокнота: боковая навигация по сферам, быстрые подборки «Сегодня», «Внимание» и «До ситуации», живые карточки фокуса и интерактивное колесо баланса.
- Основной список теперь сгруппирован по статусам. Низкие статусы и завершённые задачи опускаются вниз; внутри статуса задачи ранжируются по отметке «разобрать первой», сроку и важности.
- На главном экране показывается сегодняшняя дата и отдельное окно задач, которые требуют внимания: отмеченные первыми, просроченные, сегодняшние и важные.
- Добавлены время и длительность задачи, ссылка на Google Calendar и экспорт стандартного файла `.ics`.
- Добавлено временное хранилище «До ситуации» с текстом условия возврата. Сфера «Здоровье» автоматически добавляется в существующий каталог.
- Форма новой задачи полностью очищается после успешного сохранения. Все новые поля сохраняются в JSON задачи Supabase и совместимы со старыми данными без миграции таблиц.
- Проверено: lint, production build и локальный API-сценарий `создание со временем → временное хранилище → возврат в план → удаление`.
- Версия опубликована через GitHub/Vercel по постоянному адресу. В Production загрузились все 20 облачных задач; на ширине телефона 375 px нет переполнения страницы, а в консоли браузера нет ошибок.

Следующий шаг: начать пользоваться быстрыми подборками и постепенно задавать время тем задачам, которые нужно вывести в календарь.

## 2026-09-01 — Группировка по направлениям

- При выборе конкретной сферы задачи внутри каждого статуса разделяются заголовками направлений.
- Для задач, не закреплённых за направлением, выводится отдельная группа «Без направления».
- В заголовке направления отображается количество задач; сортировка по сроку и важности сохраняется.
- Проверено: `npm run lint` и `npm run build` завершились успешно.

## 2026-09-01 — Быстрая отметка первой задачи

- Галочка «разобрать первой» обновляется в интерфейсе мгновенно, а сохранение в Supabase выполняется в фоне.
- При ошибке синхронизации отметка возвращается в исходное состояние и показывается сообщение об ошибке.
- Область нажатия увеличена до 40 px на компьютере и 44 px на телефоне; открытие карточки задачи остаётся отдельным действием.
- Проверено: `npm run lint` и `npm run build` завершились успешно.

## 2026-09-02 — Подсчёт активных задач

- Задачи во временном хранилище «До ситуации» исключены из общего количества активных задач.
- Это же правило применяется к колесу баланса, счётчикам сфер и подборке задач на сегодня.

## 2026-09-02 — Быстрый редактор и переход к задачам

- Убрано принудительное пересоздание редактора после изменения ревизии задачи: окно больше не запускает повторную анимацию открытия после сохранения.
- Клик по блокноту, сфере в боковом меню или сфере в колесе сразу фильтрует список и прокручивает страницу к перечню задач.
- Для длинных списков включена отложенная отрисовка невидимых секций; на сенсорных устройствах отключены тяжёлые hover-эффекты, а мобильные тени и размытие упрощены.
- Проверено: `npm run lint`, TypeScript и production build завершились успешно.

## 2026-09-02 — Фокус дня и цифровое колесо баланса

- «Фокус дня» оформлен как набор крупных бумажных стикеров: одновременно показывается до пяти задач в неровной раскладке из двух рядов.
- Если задач внимания больше пяти, они разбиваются на страницы и доступны горизонтальной прокруткой, стрелками на компьютере и свайпом на телефоне.
- «Колесо баланса» переведено в контрастный цифровой стиль: светящаяся орбита, полупрозрачные плитки сфер, цветные индикаторы загрузки и отдельное выделение задач на сегодня.
- Колесо и каждая сфера остаются интерактивными и переводят к соответствующему списку задач; добавлены заметные состояния наведения, нажатия и клавиатурного фокуса.
- Для дашборда, кнопок, форм и диалогов закреплён единый системный шрифтовой стек с одинаковым отображением на компьютере и телефоне.
- Проверено: `npm run lint`, TypeScript и production build успешны; при мобильной ширине 390 px горизонтального переполнения страницы нет.
- Версия опубликована через GitHub/Vercel. На рабочем адресе 13 фокус-задач разбились на страницы `5 + 5 + 3`; переход стрелкой и новая интерактивная разметка восьми сфер проверены без ошибок браузера.
